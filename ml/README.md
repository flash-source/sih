# ML Setup
1. `python -m venv venv && source venv/bin/activate`
2. `pip install -r requirements.txt`
3. Place `Projects_Report.csv` in `data/`
4. Run `jupyter notebook` and open `notebooks/risk_prediction.ipynb`
